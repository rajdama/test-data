import { AgentExecuteOptions, AgentResult } from "@/types/agent";
import { LogLine } from "@/types/log";
import { StagehandPage } from "../StagehandPage";
import { LLMClient } from "../llm/LLMClient";
import { ToolSet } from "ai";
export declare class StagehandAgentHandler {
    private stagehandPage;
    private logger;
    private llmClient;
    private executionModel?;
    private systemInstructions?;
    private mcpTools?;
    constructor(stagehandPage: StagehandPage, logger: (message: LogLine) => void, llmClient: LLMClient, executionModel?: string, systemInstructions?: string, mcpTools?: ToolSet);
    execute(instructionOrOptions: string | AgentExecuteOptions): Promise<AgentResult>;
    private buildSystemPrompt;
    private createTools;
}
